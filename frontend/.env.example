# 前端环境变量示例
# 复制为 .env.production 并修改为实际后端地址后执行构建
# 或在构建时直接传入：VITE_API_BASE_URL=https://api.example.com npm run build

# 后端 API 地址（生产环境）
# 开发模式下留空即可，Vite 会通过 proxy 代理 /api 到后端
VITE_API_BASE_URL=https://api.example.com
